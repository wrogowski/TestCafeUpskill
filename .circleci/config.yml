# This config was automatically generated from your source code
# Stacks detected: deps:node:.
version: 2.1
orbs:
  node: circleci/node@5
jobs:
  test:
    docker:
      - image: testcafe/testcafe:latest
    parallelism: 2
    steps:
      - checkout
      - node/install-packages
      - run:
          name: Set Chromium Path
          command: export PERCY_BROWSER_PATH=./project/node_modules/@percy/core/.local-chromium/929511/chrome-linux/chrome
      - run:
          name: Install dependencies and run tests
          command: |
            npm install
            npm run test:ci:percy
      - store_test_results:
          path: /tmp/test-results
workflows:
  e2e-test:
    jobs:
      - test
